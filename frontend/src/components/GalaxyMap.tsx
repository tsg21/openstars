/**
 * Galaxy map placeholder — will be replaced with a Canvas 2D renderer in Step 4.
 */
export function GalaxyMap() {
  return (
    <div className="flex flex-1 items-center justify-center bg-[var(--color-map-bg)]">
      <span className="text-sm text-muted-foreground">
        Galaxy Map (Canvas — Step 4)
      </span>
    </div>
  );
}
