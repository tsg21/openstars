services:
  frontend:
    build: ./frontend
    pull_policy: build
    ports:
      - "3000:8080"
    environment:
      - BACKEND_URL=http://backend:8080

  backend:
    build: ./backend
    pull_policy: build
    ports:
      - "8080:8080"
    environment:
      - STORAGE_BACKEND=local
      - GAME_DATA_PATH=/data
    volumes:
      - ./local-data:/data
